## Backend env (example)
#
# Copy to: app/backend/.env
#

APP_ENV=dev
LOG_LEVEL=INFO

# In prod you typically want this false so missing Databricks config fails loudly.
ALLOW_MOCK_DATA=true

# Databricks SQL Warehouse connectivity
# Prefer using Apps ambient auth where available:
# - DATABRICKS_HOST / DATABRICKS_TOKEN
# - DATABRICKS_SQL_WAREHOUSE_HTTP_PATH
#
# Or explicit connector vars:
# DATABRICKS_SERVER_HOSTNAME=adb-<id>.<region>.azuredatabricks.net
# DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/<warehouse_id>
# DATABRICKS_ACCESS_TOKEN=<pat_or_oauth_token>
#
# Or OAuth M2M (Apps often injects these):
# DATABRICKS_CLIENT_ID=<uuid>
# DATABRICKS_CLIENT_SECRET=<secret>

# Data location
DATABRICKS_CATALOG=rtpa_catalog
DATABRICKS_SCHEMA_GOLD=credentialing_gold

# Optional CORS override (comma-separated)
# CORS_ALLOW_ORIGINS=http://localhost:5173

